# 《烈焰除蟑：火线守卫》开发准则

**文档版本**：1.0.0  
**最后更新**：2026-07-18  
**适用范围**：所有后续功能开发

---

## 一、核心原则

> **引擎只做编排，不做实现。每个功能一个模块，每个模块一个文件。**

`engine.ts` 的角色是**调度中心**（Orchestrator），不是**万能口袋**（God Object）。它的职责仅限于：

1. 实例化各功能模块
2. 在主循环中按顺序调用模块的 `update()` / `render()` 方法
3. 处理模块通过回调上报的副作用（粒子、音效、振动、UI 通知等）
4. 管理游戏状态机（MENU → PLAYING → PAUSED → GAME_OVER 等）

**禁止**在 `engine.ts` 中直接编写新功能的业务逻辑。

---

## 二、游戏引擎架构

### 2.1 架构图

```
                         ┌──────────────────────┐
                         │     engine.ts         │
                         │   （调度中心 ~3100行）  │
                         │                       │
                         │  ┌─────────────────┐  │
                         │  │ 构造函数：实例化  │  │
                         │  │ 所有模块 + 注入回调│  │
                         │  └─────────────────┘  │
                         │  ┌─────────────────┐  │
                         │  │ update() 主循环  │  │
                         │  │ 按序调用各模块    │  │
                         │  └─────────────────┘  │
                         │  ┌─────────────────┐  │
                         │  │ render() 主渲染  │  │
                         │  │ 调用静态渲染方法  │  │
                         │  └─────────────────┘  │
                         └──────┬───────────────┘
                                │
            ┌───────────────────┼───────────────────┐
            │ 委托调用           │ 回调通知           │ 静态调用
            ▼                   ▼                   ▼
    ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
    │ 实例模块      │   │ engine.ts    │   │ 静态渲染模块  │
    │ (有状态)      │   │ 处理副作用    │   │ (无状态)      │
    │              │   │              │   │              │
    │ WaveManager  │   │ 粒子生成      │   │ RoachRenderer│
    │ BossBattle   │   │ 音效播放      │   │ BgRenderer   │
    │ FanSystem    │   │ 屏幕震动      │   │ DropRenderer │
    │ StickySystem │   │ UI 状态同步   │   │ RenderUtils  │
    │ Consumable   │   │ 存档保存      │   │ NurseRenderer│
    │ ...          │   │ ...          │   │ ...          │
    └──────────────┘   └──────────────┘   └──────────────┘
```

### 2.2 模块分类

| 类型 | 特征 | 通信方式 | 示例 |
|------|------|------|------|
| **实例模块**（有状态） | 在引擎构造函数中 `new` 创建，维护自身状态 | 构造函数注入 Config + Callbacks | `WaveManager`、`FanSystem`、`ConsumableSystem` |
| **静态模块**（无状态） | 纯静态方法，不保存状态，通过参数接收数据 | 直接调用静态方法，传入所需数据 | `RoachRenderer`、`BackgroundRenderer`、`ParticleSpawner` |

### 2.3 当前模块清单

```
src/game/engine/
├── engine.ts                       # 主引擎（调度中心）
├── index.ts                        # 模块导出入口
│
├── achievement/    AchievementSystem    # 成就检测
├── ai/             RoachAISystem        # 蟑螂 AI（移动、寻路、攻击）
├── aiming/         AimingSystem         # 投掷武器瞄准
├── boss/           BossBattleSystem     # Boss 战斗（阶段、虫卵、死亡序列）
├── collision/      CollisionSystem      # 碰撞检测（火焰伤害、防线突破）
├── consumable/     ConsumableSystem     # 消耗品（购买、使用、冷却、自动使用）
├── economy/        EconomyManager       # 经济系统（击杀奖励、倍数计算）
├── fan/            FanSystem            # 风扇武器（激活、减速、击退）
├── insecticide/    InsecticideSystem    # 杀虫喷雾（毒气、锥形伤害）
├── particle/       ParticleSystem       # 粒子生命周期管理
│                   ParticleSpawner      # 粒子生成器（10 种静态方法）
├── poison/         PoisonSystem         # 毒雾系统（中毒 DoT、爆炸）
├── radar/          RadarLaserSystem     # 雷达激光（自动追踪、连射）
├── render/         BackgroundRenderer   # 背景渲染
│                   DropRenderer         # 掉落物渲染
│                   NurseRenderer        # 护士蟑螂治疗特效
│                   RenderUtils          # 特殊武器渲染工具
│                   RoachRenderer        # 蟑螂实体渲染
├── save/           SaveSystem           # 存档系统
├── sticky/         StickySystem         # 粘板系统（放置、效果、包裹）
├── swatter/        SwatterSystem        # 电蚊拍（全屏破甲、麻痹）
├── throwable/      ThrowableSystem      # 投掷物（飞行轨迹、落地效果）
├── triple/         TripleFlameSystem    # 三重火焰
├── wave/           WaveManager          # 波次管理（生成、计数、胜利）
├── weapon/         WeaponSystem         # 武器掉落物
└── weather/        WeatherSystem        # 天气效果
```

---

## 三、模块开发模式

### 模式 A：实例模块（有状态模块）

**适用场景**：需要维护自身状态的功能（计时器、冷却、阶段切换等）。

**模板结构**：

```typescript
/**
 * @fileoverview [功能名称]模块
 * @description [一句话描述模块职责]
 */

import { /* 需要的类型 */ } from '../../types';

// ========== Config 接口 ==========
/** 模块配置：从引擎读取的只读/可读数据 */
export interface XxxSystemConfig {
  /** 画布宽度 */
  canvasWidth: number;
  /** 防御线 Y 坐标（用 getter 函数保证实时性） */
  defenseLineY: () => number;
  // ... 其他配置
}

// ========== Callbacks 接口 ==========
/** 模块回调：向引擎上报副作用 */
export interface XxxSystemCallbacks {
  /** 添加浮动文字 */
  onAddFloatingText?: (x: number, y: number, text: string, color: string) => void;
  /** 播放音效 */
  onPlaySound?: (name: string) => void;
  /** 屏幕震动 */
  onScreenShake?: (amount: number) => void;
  /** 添加粒子 */
  onAddParticle?: (particle: any) => void;
  /** 更新经济 */
  onEconomyUpdate?: (economy: any) => void;
  /** 更新防线 */
  onDefenseUpdate?: (hp: number, maxHp: number) => void;
  // ... 其他回调
}

// ========== 模块类 ==========
export class XxxSystem {
  private config: XxxSystemConfig;
  private callbacks: XxxSystemCallbacks;

  constructor(config: XxxSystemConfig, callbacks: XxxSystemCallbacks) {
    this.config = config;
    this.callbacks = callbacks;
    // 初始化内部状态
  }

  /** 每帧更新 */
  update(dt: number, time: number): void {
    // 核心逻辑
  }

  /** 重置状态 */
  reset(): void {
    // 清理状态
  }
}
```

**在 engine.ts 中的集成方式**：

```typescript
// 1. 构造函数中实例化
this.xxxSystem = new XxxSystem(
  {
    canvasWidth: this.canvas.width,
    defenseLineY: () => this.defenseLineY,
  },
  {
    onAddFloatingText: (x, y, text, color) => { this.addFloatingText(x, y, text, color); },
    onPlaySound: (name) => { this.audio.play(name); },
    onScreenShake: (amount) => { this.screenShake = amount; },
  }
);

// 2. update() 主循环中调用
this.xxxSystem.update(this.dt, this.time);

// 3. resetGame() 中重置
this.xxxSystem.reset();
```

### 模式 B：静态渲染模块（无状态模块）

**适用场景**：纯渲染逻辑，不维护状态，通过参数接收所有数据。

**模板结构**：

```typescript
/**
 * @fileoverview [渲染功能名称]渲染模块
 * @description [一句话描述]
 */

// ========== Config 接口 ==========
export interface XxxRenderConfig {
  /** 需要渲染的数据 */
  data: SomeType[];
  /** 图片资源 */
  images: Record<string, HTMLImageElement>;
  /** 时间戳 */
  time: number;
  // ... 其他参数
}

// ========== 静态方法 ==========
export class XxxRenderer {
  /**
   * 渲染 [功能名称]
   * @param config - 渲染配置
   * @param ctx - 画布上下文
   */
  static render(config: XxxRenderConfig, ctx: CanvasRenderingContext2D): void {
    // 纯渲染逻辑，不修改任何状态
  }
}
```

**在 engine.ts 中的集成方式**：

```typescript
// render() 方法中直接调用静态方法
XxxRenderer.render(
  {
    data: this.xxxData,
    images: this.images,
    time: this.time,
  },
  this.ctx
);
```

---

## 四、开发流程

### 4.1 新功能开发步骤

```
步骤 1 ──→ 步骤 2 ──→ 步骤 3 ──→ 步骤 4 ──→ 步骤 5 ──→ 步骤 6
分析需求   创建模块   定义接口   实现逻辑   集成到引擎  验证
```

| 步骤 | 操作 | 产出 |
|:---:|------|------|
| 1 | **分析需求**：确定功能属于哪种模块类型（实例 / 静态） | 设计决策 |
| 2 | **创建模块文件**：在 `engine/` 下创建子目录和文件 | `engine/xxx/XxxSystem.ts` |
| 3 | **定义接口**：Config 接口（入参）+ Callbacks 接口（出参） | 两个 interface |
| 4 | **实现逻辑**：编写模块核心代码，通过 `this.callbacks` 上报副作用 | 完整模块代码 |
| 5 | **集成到引擎**：在 `engine.ts` 构造函数中实例化，在 `update()`/`render()` 中调用 | 3 处引擎修改 |
| 6 | **验证**：`npx tsc --noEmit` + `npx vite build` 必须通过 | 编译 + 构建成功 |

### 4.2 引擎修改清单

每次新增模块，只需在 `engine.ts` 中修改 **3 个位置**：

| 位置 | 操作 | 代码量 |
|------|------|:---:|
| 构造函数 | `new XxxSystem(config, callbacks)` | ~15 行 |
| `update()` | `this.xxxSystem.update(dt, time)` | 1 行 |
| `resetGame()` | `this.xxxSystem.reset()` | 1 行 |

如果是静态渲染模块，只需在 `render()` 中加 1 行调用。

---

## 五、回调接口规范

### 5.1 标准回调命名

所有回调统一使用 `on` 前缀，动词后置：

```typescript
// ✅ 正确
onAddFloatingText    // 添加浮动文字
onPlaySound          // 播放音效
onScreenShake        // 屏幕震动
onEconomyUpdate      // 更新经济
onDefenseUpdate      // 更新防线
onPlayerUpdate       // 更新玩家
onStateChange        // 切换状态
onSpawnParticle      // 生成粒子

// ❌ 错误
addFloatingText      // 缺少 on 前缀
handleFloatingText   // 不明确
onFloatingTextAdd    // 动词后置不符合习惯
```

### 5.2 回调设计原则

1. **回调必须可选**（`?:`）：模块不应假设回调一定存在，调用前检查 `this.callbacks.xxx?.()`
2. **回调不应包含业务逻辑**：回调只负责通知引擎，具体处理由引擎完成
3. **避免回调地狱**：每个模块的回调数量控制在 5-15 个以内
4. **箭头函数包装**：在引擎中注入回调时，如需访问运行时状态，必须用箭头函数包装

```typescript
// ✅ 正确：箭头函数包装，运行时动态读取
onGetRoaches: () => this.roaches

// ❌ 错误：直接引用，捕获构造时的值
onGetRoaches: this.roaches
```

### 5.3 数据传递方向

```
引擎 ──Config接口──→ 模块（读取数据）
引擎 ←──Callbacks── 模块（上报副作用）
```

- **Config**：引擎 → 模块，传递模块需要读取的数据
- **Callbacks**：模块 → 引擎，模块通知引擎执行副作用

---

## 六、代码规范

### 6.1 文件组织

```
engine/xxx/
├── XxxSystem.ts        # 实例模块（有状态逻辑）
└── XxxRenderer.ts      # 静态渲染模块（如有渲染需求）
```

### 6.2 注释规范

每个模块文件必须包含：

```typescript
/**
 * @fileoverview [功能名称]模块
 * @description [一句话描述模块职责]
 */
```

每个公开方法必须包含 JSDoc：

```typescript
/**
 * 更新风扇状态：递减计时器、更新叶片角度、对范围内蟑螂施加减速/击退
 * @param dt - 帧间隔时间（秒）
 * @param roaches - 当前所有蟑螂列表
 */
update(dt: number, roaches: Roach[]): void { ... }
```

### 6.3 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 模块类 | `XxxSystem`（实例）/ `XxxRenderer`（静态） | `FanSystem`、`RoachRenderer` |
| Config 接口 | `XxxSystemConfig` / `XxxRenderConfig` | `FanSystemConfig` |
| Callbacks 接口 | `XxxSystemCallbacks` | `BossBattleCallbacks` |
| 私有属性 | `private` 前缀 | `private config: FanSystemConfig` |
| 回调方法 | `on` + 动词 + 名词 | `onAddFloatingText` |

---

## 七、反面案例

### ❌ 错误做法：直接在 engine.ts 中写功能

```typescript
// ❌ 不要这样做！
update() {
  // 直接在 engine.ts 中写风扇逻辑
  if (this.fanActive) {
    this.fanTimer -= dt;
    if (this.fanTimer <= 0) {
      this.fanActive = false;
      this.fanTimer = 0;
    }
    // 风扇效果：减速蟑螂
    for (const roach of this.roaches) {
      if (roach.x > fanX - fanRadius && roach.x < fanX + fanRadius) {
        roach.speed *= 0.5;
      }
    }
  }
  // ... 更多功能直接堆在这里
}
```

**问题**：`engine.ts` 会不断膨胀，最终回到 9700 行的噩梦。

### ✅ 正确做法：提取为独立模块

```typescript
// ✅ engine.ts 中只需一行
this.fanSystem.update(dt, this.roaches);

// ✅ 所有逻辑在 FanSystem.ts 中
```

---

## 八、代码审查清单

提交代码前，逐项检查：

- [ ] 新功能代码在独立模块文件中，不在 `engine.ts` 中
- [ ] 模块通过 Config 接口接收数据，通过 Callbacks 接口上报副作用
- [ ] 所有回调使用 `?.()` 可选调用，防止空引用
- [ ] 引擎中注入回调时使用箭头函数包装（如需要运行时状态）
- [ ] 模块提供 `reset()` 方法（如有状态），并在 `engine.ts` 的 `resetGame()` 中调用
- [ ] `updateConfig()` 方法支持动态更新配置（如需要）
- [ ] 所有公开方法有 JSDoc 注释
- [ ] `index.ts` 中导出新模块
- [ ] `npx tsc --noEmit` 零错误
- [ ] `npx vite build` 构建成功
- [ ] 更新 `引擎迁移.md` 变更记录

---

## 九、快速参考

### 创建新实例模块的命令清单

```bash
# 1. 创建模块目录和文件
mkdir -p src/game/engine/xxx
touch src/game/engine/xxx/XxxSystem.ts

# 2. 编写模块（参考模式 A 模板）

# 3. 在 engine.ts 中集成（3 处修改）
#    - 构造函数：new XxxSystem(config, callbacks)
#    - update()：this.xxxSystem.update(dt, time)
#    - resetGame()：this.xxxSystem.reset()

# 4. 在 index.ts 中导出
#    export { XxxSystem } from './xxx/XxxSystem';

# 5. 验证
npx tsc --noEmit && npx vite build
```

### 常用回调速查

| 回调 | 签名 | 用途 |
|------|------|------|
| `onAddFloatingText` | `(x, y, text, color) => void` | 战斗文字提示 |
| `onPlaySound` | `(name: string) => void` | 播放音效 |
| `onScreenShake` | `(amount: number) => void` | 屏幕震动 |
| `onAddParticle` | `(particle) => void` | 添加粒子 |
| `onEconomyUpdate` | `(economy) => void` | 更新金钱/击杀 |
| `onDefenseUpdate` | `(hp, maxHp) => void` | 更新防线血量 |
| `onPlayerUpdate` | `(player) => void` | 更新玩家状态 |
| `onStateChange` | `(state: GameState) => void` | 切换游戏状态 |
| `onSaveProgress` | `() => void` | 保存存档 |
| `onVibrate` | `(pattern) => void` | 设备振动 |